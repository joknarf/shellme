# shellme

`shellme` plays scripted commands in a interactive shell as it is done by a human.


```
# shellme "echo This is a demo" "ls -l"
```

# usage

```
shellme -f <commands_file>
shellme <cmd>...
```
